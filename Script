local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- CONFIG GLOBALS
getgenv().Aimbot = false
getgenv().ShowFOV = false
getgenv().AimbotFOV = 150
getgenv().FOVColor = Color3.fromRGB(0, 255, 255)
getgenv().CustomSpeed = 16 
getgenv().Noclip = false
getgenv().FlyEnabled = false
getgenv().FlySpeed = 50
getgenv().DoorBypass = false
getgenv().ESPEnabled = false

-- =========================
-- PERSISTENT SPEED
-- =========================
LocalPlayer.CharacterAdded:Connect(function(char)
    local hum = char:WaitForChild("Humanoid", 10)
    hum.WalkSpeed = getgenv().CustomSpeed
end)

-- =========================
-- FLIGHT & NOCLIP ENGINE
-- =========================
local BodyGyro = Instance.new("BodyGyro")
local BodyVelocity = Instance.new("BodyVelocity")
BodyGyro.P = 9e4
BodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)

RunService.RenderStepped:Connect(function()
    -- Flight Logic
    if getgenv().FlyEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local HRP = LocalPlayer.Character.HumanoidRootPart
        BodyGyro.Parent = HRP
        BodyVelocity.Parent = HRP
        BodyGyro.CFrame = Camera.CFrame
        local Direction = Vector3.new(0,0,0)
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then Direction = Direction + Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then Direction = Direction - Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then Direction = Direction - Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then Direction = Direction + Camera.CFrame.RightVector end
        BodyVelocity.Velocity = Direction * getgenv().FlySpeed
    else
        BodyGyro.Parent = nil
        BodyVelocity.Parent = nil
    end

    -- FOV Visuals
    FOVCircle.Visible = getgenv().ShowFOV
    FOVCircle.Radius = getgenv().AimbotFOV
    FOVCircle.Color = getgenv().FOVColor
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)

    -- ESP Logic
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local highlight = p.Character:FindFirstChild("ESP_Highlight")
            if getgenv().ESPEnabled then
                if not highlight then
                    highlight = Instance.new("Highlight", p.Character)
                    highlight.Name = "ESP_Highlight"
                    highlight.FillTransparency = 0.5
                end
                highlight.Enabled = true
                highlight.FillColor = p.TeamColor.Color
            elseif highlight then
                highlight.Enabled = false
            end
        end
    end
end)

-- Noclip Stepped
RunService.Stepped:Connect(function()
    if getgenv().Noclip and LocalPlayer.Character then
        for _, v in pairs(LocalPlayer.Character:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
    end
end)

-- =========================
-- UTILITIES: DOOR BYPASS
-- =========================
task.spawn(function()
    while task.wait(0.5) do
        if getgenv().DoorBypass then
            for _, v in pairs(workspace:GetDescendants()) do
                if (v.Name == "Door" or v.Name == "Gate") and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    local Dist = (v:IsA("Model") and v.PrimaryPart and (v.PrimaryPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude) or 100
                    if Dist < 15 then
                        local cd = v:FindFirstChildOfClass("ClickDetector")
                        if cd then fireclickdetector(cd) end
                    end
                end
            end
        end
    end
end)

-- =========================
-- UI WINDOW
-- =========================
local Window = Rayfield:CreateWindow({
    Name = "Zenhub Ultra | V9.0 Master",
    LoadingTitle = "Assembling All Modules...",
})

-- COMBAT TAB
local CombatTab = Window:CreateTab("Combat")
CombatTab:CreateSection("— Aimbot —")
CombatTab:CreateToggle({Name = "Enable Aimbot", CurrentValue = false, Callback = function(v) getgenv().Aimbot = v end})
CombatTab:CreateToggle({Name = "Show FOV Circle", CurrentValue = false, Callback = function(v) getgenv().ShowFOV = v end})
CombatTab:CreateSlider({Name = "FOV Size", Range = {30, 800}, Increment = 10, CurrentValue = 150, Callback = function(v) getgenv().AimbotFOV = v end})
CombatTab:CreateColorPicker({Name = "FOV Color", Color = Color3.fromRGB(0, 255, 255), Callback = function(v) getgenv().FOVColor = v end})

-- MOVEMENT TAB
local MovementTab = Window:CreateTab("Movement")
MovementTab:CreateSection("— Speed —")
MovementTab:CreateSlider({Name = "WalkSpeed", Range = {16, 150}, Increment = 1, CurrentValue = 16, Callback = function(v)
    getgenv().CustomSpeed = v
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then LocalPlayer.Character.Humanoid.WalkSpeed = v end
end})

MovementTab:CreateSection("— Flight —")
MovementTab:CreateToggle({Name = "Flight Mode", CurrentValue = false, Callback = function(v) getgenv().FlyEnabled = v end})
MovementTab:CreateSlider({Name = "Flight Speed", Range = {10, 300}, Increment = 5, CurrentValue = 50, Callback = function(v) getgenv().FlySpeed = v end})

MovementTab:CreateSection("— Physics —")
MovementTab:CreateToggle({Name = "Noclip", CurrentValue = false, Callback = function(v) getgenv().Noclip = v end})

-- VISUALS TAB
local VisualsTab = Window:CreateTab("Visuals")
VisualsTab:CreateToggle({Name = "Player ESP", CurrentValue = false, Callback = function(v) getgenv().ESPEnabled = v end})
VisualsTab:CreateButton({Name = "Enable Fullbright", Callback = function()
    Lighting.Brightness = 2
    Lighting.ClockTime = 14
    Lighting.GlobalShadows = false
end})

-- UTILITIES TAB
local UtilityTab = Window:CreateTab("Utilities")
UtilityTab:CreateToggle({Name = "Auto-Door Bypass", CurrentValue = false, Callback = function(v) getgenv().DoorBypass = v end})
UtilityTab:CreateButton({Name = "Rejoin Server", Callback = function() game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId) end})
