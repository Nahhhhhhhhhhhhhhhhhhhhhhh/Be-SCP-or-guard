local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")

-- CONFIG
getgenv().Aimbot = false
getgenv().AimbotTeamCheck = false
getgenv().ShowFOV = false
getgenv().AimbotFOV = 150
getgenv().TargetPart = "Head" 
getgenv().MaxDistance = 500 
getgenv().SpecificSCPTarget = "All"
getgenv().SpecificTeamTarget = "All"
getgenv().ManualOverride = true -- The "Swipe to Deactivate" logic

local IsSwiping = false

-- DETECT MANUAL CAMERA MOVEMENT
UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Gamepad2 then
        IsSwiping = true
        task.delay(0.1, function() IsSwiping = false end) -- Short delay before re-locking
    end
end)

-- TARGETING ENGINE
local function GetClosestTarget()
    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then return nil end
    if getgenv().ManualOverride and IsSwiping then return nil end -- Stop aimbot if user is swiping

    local Target = nil
    local MaxDistFOV = getgenv().AimbotFOV
    local MyPos = LocalPlayer.Character.HumanoidRootPart.Position

    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("Humanoid") and v.Character.Humanoid.Health > 0 then
            
            -- Team Dropdown Filter
            if getgenv().SpecificTeamTarget ~= "All" and v.Team and v.Team.Name ~= getgenv().SpecificTeamTarget then continue end
            
            -- SCP Dropdown Filter
            if v.Team and v.Team.Name == "SCP" and getgenv().SpecificSCPTarget ~= "All" then
                if v.Character.Name ~= getgenv().SpecificSCPTarget then continue end
            end

            local Part = v.Character:FindFirstChild(getgenv().TargetPart)
            if Part then
                local WorldDist = (Part.Position - MyPos).Magnitude
                if WorldDist <= getgenv().MaxDistance then
                    local Pos, OnScreen = Camera:WorldToViewportPoint(Part.Position)
                    if OnScreen then
                        local ScreenDist = (Vector2.new(Pos.X, Pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                        if ScreenDist < MaxDistFOV then
                            Target = v
                            MaxDistFOV = ScreenDist
                        end
                    end
                end
            end
        end
    end
    return Target
end

-- RUNTIME
RunService.RenderStepped:Connect(function()
    if getgenv().Aimbot then
        local Target = GetClosestTarget()
        if Target and Target.Character and Target.Character:FindFirstChild(getgenv().TargetPart) then
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, Target.Character[getgenv().TargetPart].Position)
        end
    end
end)

-- UI WINDOW
local Window = Rayfield:CreateWindow({
    Name = "Zenhub Premium | Swipe Edition",
    LoadingTitle = "Loading Assets...",
    ConfigurationSaving = { Enabled = false }
})

local CombatTab = Window:CreateTab("Combat")

CombatTab:CreateSection("Camera Logic")
CombatTab:CreateToggle({
    Name = "Swipe to Deactivate", 
    CurrentValue = true, 
    Info = "Aimbot stops while you are manually moving the camera.",
    Callback = function(v) getgenv().ManualOverride = v end
})
CombatTab:CreateToggle({Name = "Enable Aimbot", CurrentValue = false, Callback = function(v) getgenv().Aimbot = v end})

CombatTab:CreateSection("Target Filtering")
CombatTab:CreateDropdown({
    Name = "Target Specific Team",
    Options = {"All", "Guard", "SCP", "Class-D", "Chaos Insurgency"},
    CurrentOption = {"All"},
    Callback = function(Option) getgenv().SpecificTeamTarget = Option[1] end,
})

CombatTab:CreateDropdown({
    Name = "Target Specific SCP",
    Options = {"All", "SCP-173", "SCP-096", "SCP-049", "SCP-106", "SCP-939", "SCP-035"},
    CurrentOption = {"All"},
    Callback = function(Option) getgenv().SpecificSCPTarget = Option[1] end,
})

CombatTab:CreateSection("Adjustments")
CombatTab:CreateDropdown({
    Name = "Target Bone",
    Options = {"Head", "HumanoidRootPart"},
    CurrentOption = {"Head"},
    Callback = function(Option) getgenv().TargetPart = Option[1] end,
})

CombatTab:CreateSlider({
    Name = "Lock Distance",
    Range = {50, 2000},
    Increment = 50,
    CurrentValue = 500,
    Callback = function(v) getgenv().MaxDistance = v end
})

local VisualsTab = Window:CreateTab("Visuals")
VisualsTab:CreateSection("ESP")
VisualsTab:CreateToggle({Name = "Master ESP", CurrentValue = false, Callback = function(v) ESP_Config.Enabled = v end})
