-- STEP 1: Define the URL clearly
local RayfieldURL = "https://raw.githubusercontent.com/SiriusSoftwareLtd/Rayfield/main/source.lua"

-- STEP 2: Fetch the code (Using a protected call to stop crashes)
local Success, SourceCode = pcall(function()
    return game:HttpGet(RayfieldURL)
end)

if not Success or SourceCode == nil then
    warn("ERROR: Could not reach the UI Library. Check your Wi-Fi!")
    return
end

-- STEP 3: Wait a moment for the script to 'settle' in memory
task.wait(0.5)

-- STEP 4: Load the library
local Rayfield = loadstring(SourceCode)()

-- STEP 5: Build the Window
local Window = Rayfield:CreateWindow({
   Name = "Site-Logistics | SCP Absolute Hub",
   LoadingTitle = "Bypassing Security...",
   LoadingSubtitle = "Mobile Protocol - Gemini",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "SCP_Guard_Config",
      FileName = "Main"
   }
})

-- SERVICES
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")

-- CONFIG
getgenv().Aimbot = false
getgenv().SilentAim = false
getgenv().AimbotFOV = 150
getgenv().ShowFOV = false
getgenv().Noclip = false

local ESP_Config = {
    Enabled = false,
    ShowNames = false,
    NameScale = 14,
    HighlightTransparency = 0.5
}

-- FOV CIRCLE
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 2
FOVCircle.Color = Color3.fromRGB(255, 255, 255)
FOVCircle.Transparency = 0.7

-- TARGETING LOGIC
local function GetClosestToMouse()
    local Target = nil
    local MaxDist = getgenv().AimbotFOV
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            local Pos, OnScreen = Camera:WorldToViewportPoint(v.Character.HumanoidRootPart.Position)
            if OnScreen then
                local Dist = (Vector2.new(Pos.X, Pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                if Dist < MaxDist then
                    Target = v
                    MaxDist = Dist
                end
            end
        end
    end
    return Target
end

-- ESP SYSTEM
local function ApplyESP(player)
    if player == LocalPlayer then return end
    local function setupESPOnChar(char)
        if not char then return end
        pcall(function()
            if char:FindFirstChild("ESPHighlight") then char.ESPHighlight:Destroy() end
            if char:FindFirstChild("Head") and char.Head:FindFirstChild("ESPNameTag") then char.Head.ESPNameTag:Destroy() end

            if ESP_Config.Enabled then
                local highlight = Instance.new("Highlight", char)
                highlight.Name = "ESPHighlight"
                highlight.FillTransparency = ESP_Config.HighlightTransparency
                if player.Team then highlight.FillColor = player.TeamColor.Color end

                if ESP_Config.ShowNames and char:FindFirstChild("Head") then
                    local bgui = Instance.new("BillboardGui", char.Head)
                    bgui.Name = "ESPNameTag"
                    bgui.Size = UDim2.new(0, 100, 0, 50)
                    bgui.StudsOffset = Vector3.new(0, 2.5, 0)
                    bgui.AlwaysOnTop = true
                    local label = Instance.new("TextLabel", bgui)
                    label.BackgroundTransparency = 1
                    label.Size = UDim2.new(1, 0, 1, 0)
                    label.Text = player.DisplayName or player.Name
                    label.TextColor3 = Color3.new(1, 1, 1)
                    label.TextSize = ESP_Config.NameScale
                    label.Font = Enum.Font.SourceSansBold
                    label.TextStrokeTransparency = 0
                end
            end
        end)
    end
    player.CharacterAdded:Connect(setupESPOnChar)
    if player.Character then setupESPOnChar(player.Character) end
end

-- METATABLE (SILENT AIM)
local mt = getrawmetatable(game)
local old = mt.__index
setreadonly(mt, false)
mt.__index = newcclosure(function(t, k)
    if not checkcaller() and k == "Hit" and getgenv().SilentAim then
        local Target = GetClosestToMouse()
        if Target then return Target.Character.HumanoidRootPart.CFrame end
    end
    return old(t, k)
end)
setreadonly(mt, true)

-- UPDATER LOOP
RunService.RenderStepped:Connect(function()
    FOVCircle.Visible = getgenv().ShowFOV
    FOVCircle.Radius = getgenv().AimbotFOV
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)

    if getgenv().Aimbot then
        local Target = GetClosestToMouse()
        if Target and Target.Character and Target.Character:FindFirstChild("Head") then
            Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, Target.Character.Head.Position), 0.1)
        end
    end
    
    if getgenv().Noclip and LocalPlayer.Character then
        for _, v in pairs(LocalPlayer.Character:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
    end
end)

-- TABS
local CombatTab = Window:CreateTab("Combat")
CombatTab:CreateToggle({Name = "Aimbot", CurrentValue = false, Flag = "Aim", Callback = function(v) getgenv().Aimbot = v end})
CombatTab:CreateToggle({Name = "Silent Aim", CurrentValue = false, Flag = "SAim", Callback = function(v) getgenv().SilentAim = v end})
CombatTab:CreateToggle({Name = "Show FOV", CurrentValue = false, Flag = "SFOV", Callback = function(v) getgenv().ShowFOV = v end})

local VisualsTab = Window:CreateTab("Visuals")
VisualsTab:CreateToggle({Name = "ESP", CurrentValue = false, Flag = "EESP", Callback = function(v) ESP_Config.Enabled = v for _,p in pairs(Players:GetPlayers()) do ApplyESP(p) end end})
VisualsTab:CreateToggle({Name = "Names", CurrentValue = false, Flag = "EName", Callback = function(v) ESP_Config.ShowNames = v for _,p in pairs(Players:GetPlayers()) do ApplyESP(p) end end})

local MovementTab = Window:CreateTab("Movement")
MovementTab:CreateSlider({Name = "WalkSpeed", Range = {16, 200}, Increment = 1, CurrentValue = 16, Flag = "WS", Callback = function(v) 
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = v 
    end
end})
MovementTab:CreateToggle({Name = "No-Clip", CurrentValue = false, Flag = "NC", Callback = function(v) getgenv().Noclip = v end})

Rayfield:LoadConfiguration()
